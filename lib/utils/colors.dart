import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primary = Color(0xFF6C5CE7);  // More modern purple
  static const primaryLight = Color(0xFFA29BFE);  // Lighter variant
  static const primaryDark = Color(0xFF5649CC);  // Darker variant

  // Secondary/Accent colors
  static const secondary = Color(0xFF00CEB4);  // Teal accent
  static const accent = Color(0xFFFD79A8);  // Pink accent

  // Background colors
  static const background = Color(0xFFF9F9FF);  // Very light purple-white
  static const surface = Color(0xFFFFFFFF);  // Pure white for cards

  // Text colors
  static const textDark = Color(0xFF2D3436);  // Dark gray for primary text
  static const textMedium = Color(0xFF636E72);  // Medium gray for secondary text
  static const textLight = Color(0xFFB2BEC3);  // Light gray for hints

  // Functional colors
  static const success = Color(0xFF00B894);  // Green
  static const warning = Color(0xFFFDCB6E);  // Yellow
  static const error = Color(0xFFD63031);  // Red
}